@import 'constants.styl'
@import 'fonts.styl'
@import 'utils.styl'


button()
  cursor pointer
  user-select none
  box-sizing border-box
  padding 10px 20px
  border colorBorder solid 1px
  border-radius radiusM
  color colorText1
  text-shadow none
  background colorBlockBg
  transition all 0.2s ease
  font-medium()
  svg-inside(1lh)
  trans()
  centered-flex-container()

  &:hover
    background mix(black, colorBlockBg, 20%)

  &[disabled]:not([disabled="false"])
    pointer-events none
    color mix(white, colorText1, 70%)
    background mix(white, colorBlockBg, 50%)
    box-shadow none

button-no-fill()
  button()

  padding-right 10px
  padding-left 10px
  border-color transparent
  color colorText2
  background transparent

  &:hover
    border-color mix(colorBlockBg, white, 20%)
    color colorText1
    background mix(colorBlockBg, white, 20%)

button-emp1()
  button()

  border-color transparent
  color colorTextInvert1
  background colorEmp1

  &:hover
    border-color transparent
    color colorTextInvert1
    background colorEmp1Hover

button-emp2()
  button-emp1()

  background colorEmp2

  &:hover
    background colorEmp2Hover


button-emp1-no-fill()
  button-emp1()

  padding-right 10px
  padding-left 10px
  border colorEmp1 1px solid
  color colorEmp1
  background transparent

  &:hover
    border-color mix(colorBlockBg, white, 20%)
    color colorEmp1
    background mix(colorEmp1, white, 20%)

button-emp2-no-fill()
  button-emp2()

  padding-right 10px
  padding-left 10px
  border colorEmp2 1px solid
  color colorEmp2
  background transparent

  &:hover
    border-color mix(colorBlockBg, white, 20%)
    color colorText1
    background mix(colorEmp2, white, 20%)


button-attention()
  button()

  border-color colorError
  background mix(colorError, transparent)

  &:hover
    border-color mix(colorError, white)
    color colorTextInvert1

button-success()
  button()

  border-color colorSuccess
  color colorTextInvert3
  background mix(colorSuccess, transparent)

  &:hover
    border-color mix(colorSuccess, white)
    color colorTextInvert1

